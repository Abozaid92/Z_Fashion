import React from "react";
import RegisterPage from "../clientPart/register";

const RegisterPageServer = () => {
  return <RegisterPage />;
};

export default RegisterPageServer;
export const metadata = {
  title: "Create an Account",
  description: "Join us today",
};
